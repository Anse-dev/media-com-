{% extends 'base.html.twig' %}

{% block title %}Artist{% endblock %}

{% block body %}
    <h1>Artist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artist.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ artist.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ artist.roles ? artist.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ artist.password }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ artist.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ artist.prenom }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ artist.name }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ artist.lastname }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ artist.adress }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ artist.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_artist_index') }}">back to list</a>

    <a href="{{ path('app_artist_edit', {'id': artist.id}) }}">edit</a>

    {{ include('artist/_delete_form.html.twig') }}
{% endblock %}
